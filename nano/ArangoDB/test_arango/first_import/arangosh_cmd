db._dropDatabase('test'); 
db._createDatabase('test');
db._useDatabase('test');
db._create('person');
db._create('proj');
db._create('org');
db._createEdgeCollection('participation');
db._createEdgeCollection('work');
db.org.save({ name : '行政院農業委員會'});
db.org.save({ name : '師範大學'});
db.org.save({ name : '中央研究院'});
db.org.save({ name : '中興大學'});
db.org.save({ name : '臺北大學'});
db.org.save({ name : '國立台灣大學'});
db.org.save({ name : '交通大學'});
db.org.save({ name : '國立成功大學'});
db.org.save({ name : '國立中興大學'});
db.org.save({ name : '國立東華大學'});
db.org.save({ name : '中研院'});
db.org.save({ name : '台灣大學'});
db.org.save({ name : '國立交通大學'});
db.org.save({ name : '中正大學'});
db.org.save({ name : '南臺科技大學'});
db.org.save({ name : '國立聯合大學'});
db.org.save({ name : '中國文化大學'});
db.org.save({ name : '台北醫學大學'});
db.org.save({ name : '中央大學'});
db.org.save({ name : '國立中正大學'});
db.org.save({ name : '國立臺北大學'});
db.org.save({ name : '國立清華大學'});
db.org.save({ name : '國立臺灣海洋大學'});
db.org.save({ name : '國立政治大學'});
db.org.save({ name : '清華大學'});
db.org.save({ name : '元智大學'});
db.org.save({ name : '國網中心'});
db.org.save({ name : '國立台灣師範大學'});
db.org.save({ name : '國立中央大學'});
db.org.save({ name : '逢甲大學'});
db.org.save({ name : '國立宜蘭大學'});
db.org.save({ name : '國立台北科技大學'});
db.org.save({ name : '國立臺灣師範大學'});
db.proj.save({ title : '應用於居家照護之智慧型互動平台－應用於醫療場域及居家照護之智慧 型互動平台- 以人工智慧為核心之腦波人機介面開發', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '基於深度學習之手勢人機介面與定位加值服務', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '結合UAV監測之智慧農業栽培支援系統', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '基於人工智能融合技術集成多元環境感測的導引及巡邏機器人', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '具深度理解之對話系統及智慧型輔助學習機器人', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '應用於ADAS特殊用途無人載具之嵌入式AI深度學習技術', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '視障人士的智慧夥伴', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '人工智慧在輔助人類學習對局遊戲之應用(2/5~5/5)(1/4)', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : 'Smart Drones之AI平台開發─智慧飛行控制', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '基於生成模型的視訊壓縮', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '深度強化式學習技術之應用研究', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '前瞻無人機智慧系統：核心技術研發與情境應用展示', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '基於人工智慧發展多元國家考試類型與評測工具之研製', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '智慧滯空飛行無人機：基地台通訊暨網路技術', keyword : 'DL,CNN,RNN'});
db.proj.save({ title : '基於深度智能之口語處理技術', keyword : 'DL,CNN,RNN'});
var person_id = db.person.save({ name : '謝易錚', email : 'yzhsieh@mail.ntou.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立臺灣海洋大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'助理教授'});
var person_id = db.person.save({ name : '李佳翰', email : 'chiahan@g2.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機系', title:'副教授'});
var person_id = db.person.save({ name : '李柏磊', email : 'pllee@ee.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機系', title:'教授'});
var person_id = db.person.save({ name : '林幸助', email : 'hjlin@dragon.nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'生命科學系', title:'教授'});
var person_id = db.person.save({ name : '王新民', email : 'whm@iis.sinica.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央研究院' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科學研究所', title:'研究員'});
var person_id = db.person.save({ name : '莊仁輝', email : 'jchuang@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系/資訊學院', title:'教授/院長'});
var person_id = db.person.save({ name : '楊叔卿', email : 'scy@mx.nthu.edu.tw/scy0120@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立清華大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'學習科學與科技研究所', title:'教授'});
var person_id = db.person.save({ name : '李聯旺', email : 'leelw@dragon.nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'機械工程系', title:'副教授'});
var person_id = db.person.save({ name : '李宏毅', email : 'hungyilee@ntu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立台灣大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊網路與多媒體研究所', title:'助理教授'});
var person_id = db.person.save({ name : '鄧俊宏', email : 'jhdeng.hank@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='元智大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'通訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '王文俊', email : 'wjwang@ee.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'講座教授'});
var person_id = db.person.save({ name : '廖文宏', email : 'whliao@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立政治大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科學系', title:'副教授'});
var person_id = db.person.save({ name : '蘇木春', email : 'muchun@csie.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程系', title:'特聘教授'});
var person_id = db.person.save({ name : '張志文', email : 'wenson@ee.ncku.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立成功大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電腦與通信工程學系', title:'副教授'});
var person_id = db.person.save({ name : '林信標', email : 'hplin@ntut.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立台北科技大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電子工程系', title:'教授'});
var person_id = db.person.save({ name : '曾煜棋', email : 'yctseng@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'講座教授'});
var person_id = db.person.save({ name : '包傑奇', email : 'jacky.baltes@ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立台灣師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'教授'});
var person_id = db.person.save({ name : '陳以錚', email : 'ejen0831@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊管理學系', title:'助理教授'});
var person_id = db.person.save({ name : '馬偉雲', email : 'ma@iis.sinica.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中研院' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科學研究所', title:'助研究員'});
var person_id = db.person.save({ name : '陳樹群', email : 'scchen@nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'水土保持學系', title:'教授兼農資學院院長'});
var person_id = db.person.save({ name : '陳柏琳', email : 'berlin@csie.ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '陳慶瀚', email : 'pierre.miat@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程系', title:'副教授'});
var person_id = db.person.save({ name : '張詠淳', email : 'changyc@tmu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='台北醫學大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'大數據科技及管理研究所', title:'助理教授'});
var person_id = db.person.save({ name : '蔡宗翰', email : 'thtsai@g.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '陳志昌', email : 'jcchen@mail.ntpu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='臺北大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程系', title:'副教授'});
var person_id = db.person.save({ name : '程登湖', email : 'tenghu@nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'機械工程系', title:'助理教授'});
var person_id = db.person.save({ name : '李宜勳', email : 'lyx30@ulive.pccu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中國文化大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'副教授'});
var person_id = db.person.save({ name : '許聞廉', email : 'hsu@iis.sinica.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中研院' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科學研究所', title:'所長'});
var person_id = db.person.save({ name : '許陳鑑', email : 'jhsu@ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立台灣師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'優聘教授兼系主任'});
var person_id = db.person.save({ name : '范國清', email : 'kcfan@csie.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'講座教授'});
var person_id = db.person.save({ name : '蕭旭峰', email : 'hillhsiao@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'副教授'});
var person_id = db.person.save({ name : '李奇育', email : 'chiyuli@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'助理教授'});
var person_id = db.person.save({ name : '王偉彥', email : 'wywang@ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立臺灣師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'教授'});
var person_id = db.person.save({ name : '黃能富', email : 'nfhuang@cs.nthu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立清華大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授兼電機資訊學院院長'});
var person_id = db.person.save({ name : '鄭淑真', email : 'kittyc@stust.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='南臺科技大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程系', title:'副教授'});
var person_id = db.person.save({ name : '蔡宗漢', email : 'han@ee.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'教授'});
var person_id = db.person.save({ name : '顏士淨', email : 'sjyen@mail.ndhu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立東華大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程研究所', title:'教授'});
var person_id = db.person.save({ name : '林政宏', email : 'brucelin@ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立臺灣師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'副教授'});
var person_id = db.person.save({ name : '陳琦玲', email : 'chiling@tari.gov.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='行政院農業委員會' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'農業試驗所農業化學組', title:'研究員'});
var person_id = db.person.save({ name : '鄭旭詠', email : 'chengsy@csie.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'教授'});
var person_id = db.person.save({ name : '謝君偉', email : 'shieh@email.ntou.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立臺灣海洋大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'教授'});
var person_id = db.person.save({ name : '范倫達', email : 'ldvan@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'副教授'});
var person_id = db.person.save({ name : '紀明德', email : 'dodowell@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立政治大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科學系', title:'副教授'});
var person_id = db.person.save({ name : '林智揚', email : 'andrewlin@saturn.yzu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='元智大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'通訊工程學系', title:'副教授'});
var person_id = db.person.save({ name : '蔣欣翰', email : 'hsinhan@ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立台灣師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'副教授'});
var person_id = db.person.save({ name : '張寶基', email : 'pcchang@ce.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'通訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '楊靜瑩', email : 'emiyang@dragon.nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'農藝學系', title:'副教授兼任農業試驗場場長'});
var person_id = db.person.save({ name : '陳永昇', email : 'yschen@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'教授'});
var person_id = db.person.save({ name : '李蔡彥', email : 'li.nccu@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立政治大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科學系', title:'特聘教授'});
var person_id = db.person.save({ name : '陳冠文', email : 'kuanwen@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'助理教授'});
var person_id = db.person.save({ name : '郭峻因', email : 'jiguo@nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電子研究所', title:'教授'});
var person_id = db.person.save({ name : '林芳邦', email : 'fplin@nchc.narl.org.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國網中心' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'', title:'資深研究員'});
var person_id = db.person.save({ name : '郭建宏', email : 'chk@ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立臺灣師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'副教授'});
var person_id = db.person.save({ name : '彭文孝', email : 'wpeng@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '徐國鎧', email : 'kkshyu@ee.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機系', title:'教授'});
var person_id = db.person.save({ name : '吳毅成', email : 'icwu@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '陳翔傑', email : 'chc@nuu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立聯合大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機工程學系', title:'助理教授'});
var person_id = db.person.save({ name : '王蒞君', email : 'lichun@g2.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機系', title:'教授'});
var person_id = db.person.save({ name : '韓仁毓', email : 'jyhan@ntu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='台灣大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'土木工程學系', title:'教授'});
var person_id = db.person.save({ name : '曾建維', email : 'darkdreams0802@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立清華大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'教學發展中心', title:'助理研究員'});
var person_id = db.person.save({ name : '阮瑞祥', email : 'run5116@ms16.hinet.net', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立聯合大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電子工程學系', title:'副教授'});
var person_id = db.person.save({ name : '陳華總', email : 'huatsung@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='逢甲大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'副教授'});
var person_id = db.person.save({ name : '帥宏翰', email : 'hhshuai@g2.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機系', title:'助理教授'});
var person_id = db.person.save({ name : '許奕婷', email : 'agroting@dragon.nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'農藝學系', title:'助理教授'});
var person_id = db.person.save({ name : '曹昱', email : 'yu.tsao@citi.sinica.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央研究院' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科技創新研究中心', title:'副研究員'});
var person_id = db.person.save({ name : '呂菁菁', email : 'luchingching2009@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='清華大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'臺灣語言研究與教學研究所', title:'副教授'});
var person_id = db.person.save({ name : '邱維辰', email : 'walon@cs.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'助理教授'});
var person_id = db.person.save({ name : '劉吉軒', email : 'jyishane.liu@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立政治大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊科學系', title:'教授兼創新與創造力研究中心主任'});
var person_id = db.person.save({ name : '施國琛', email : 'timothykshih@gmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'特聘教授'});
var person_id = db.person.save({ name : '陳宗麟', email : 'tsunglin@mail.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'機械工程系', title:'教授/系主任'});
var person_id = db.person.save({ name : '王子華', email : 'tzuhuawang@mx.nthu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立清華大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'教育與學習科技學系', title:'教授兼竹師教育學院副院長'});
var person_id = db.person.save({ name : '張俊盛', email : 'jason@nlplab.cc', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立清華大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '張添烜', email : 'tschang@twins.ee.nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電子研究所', title:'教授'});
var person_id = db.person.save({ name : '蘇柏齊', email : 'pochyisu@csie.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資工系', title:'教授/系所副主任'});
var person_id = db.person.save({ name : '王家慶', email : 'jcw@csie.ncu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立中央大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '吳政瑋', email : 'silvemoonfox@hotmail.com', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立宜蘭大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'助理教授'});
var person_id = db.person.save({ name : '黃敬群', email : 'chingchun@ccu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中正大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電機系', title:'副教授'});
var person_id = db.person.save({ name : '簡仁宗', email : 'jtchien@nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程學系', title:'教授'});
var person_id = db.person.save({ name : '楊明德', email : 'mdyang@dragon.nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'土木系', title:'計畫主持人'});
var person_id = db.person.save({ name : '杭學鳴', email : 'hmhang@nctu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立交通大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'電子工程學系', title:'教授'});
var person_id = db.person.save({ name : '林順喜', email : 'linss@csie.ntnu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='國立臺灣師範大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'資訊工程系', title:'教授'});
var person_id = db.person.save({ name : '郭寶錚', email : 'bjkuo@nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'農藝學系', title:'教授'});
var person_id = db.person.save({ name : '申雍', email : 'yshen@nchu.edu.tw', expertise : 'DL,CNN,RNN'})._id;
var org_id = db._query(`for school in org filter school.name=='中興大學' return school._id`).toArray()[0];
db.work.save(person_id, org_id,{ department:'土壤學系', title:'教授兼農資學院副院長'});
var person_id = db._query(`for per in person filter per.name=='徐國鎧' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='應用於居家照護之智慧型互動平台－應用於醫療場域及居家照護之智慧 型互動平台- 以人工智慧為核心之腦波人機介面開發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李柏磊' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='應用於居家照護之智慧型互動平台－應用於醫療場域及居家照護之智慧 型互動平台- 以人工智慧為核心之腦波人機介面開發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='范國清' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度學習之手勢人機介面與定位加值服務' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='施國琛' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度學習之手勢人機介面與定位加值服務' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='蘇柏齊' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度學習之手勢人機介面與定位加值服務' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='鄭旭詠' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度學習之手勢人機介面與定位加值服務' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳以錚' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度學習之手勢人機介面與定位加值服務' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '協同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='王蒞君' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='林信標' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳宗麟' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李佳翰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='帥宏翰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='程登湖' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李奇育' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='張志文' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='黃敬群' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='鄧俊宏' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='智慧滯空飛行無人機：基地台通訊暨網路技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='莊仁輝' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='Smart Drones之AI平台開發─智慧飛行控制' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='謝君偉' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='Smart Drones之AI平台開發─智慧飛行控制' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳永昇' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='Smart Drones之AI平台開發─智慧飛行控制' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳冠文' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='Smart Drones之AI平台開發─智慧飛行控制' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳華總' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='Smart Drones之AI平台開發─智慧飛行控制' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='彭文孝' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='Smart Drones之AI平台開發─智慧飛行控制' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='邱維辰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='Smart Drones之AI平台開發─智慧飛行控制' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='黃能富' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智慧發展多元國家考試類型與評測工具之研製' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='曾建維' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智慧發展多元國家考試類型與評測工具之研製' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人兼計畫聯絡人' }  );
var person_id = db._query(`for per in person filter per.name=='鄭淑真' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智慧發展多元國家考試類型與評測工具之研製' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='楊叔卿' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智慧發展多元國家考試類型與評測工具之研製' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='王子華' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智慧發展多元國家考試類型與評測工具之研製' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='張俊盛' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智慧發展多元國家考試類型與評測工具之研製' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='曾煜棋' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智能融合技術集成多元環境感測的導引及巡邏機器人' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='范倫達' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智能融合技術集成多元環境感測的導引及巡邏機器人' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='吳政瑋' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於人工智能融合技術集成多元環境感測的導引及巡邏機器人' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='許聞廉' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='具深度理解之對話系統及智慧型輔助學習機器人' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='馬偉雲' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='具深度理解之對話系統及智慧型輔助學習機器人' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='呂菁菁' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='具深度理解之對話系統及智慧型輔助學習機器人' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='張詠淳' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='具深度理解之對話系統及智慧型輔助學習機器人' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='吳毅成' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='深度強化式學習技術之應用研究' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳志昌' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='深度強化式學習技術之應用研究' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='簡仁宗' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='深度強化式學習技術之應用研究' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='彭文孝' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='深度強化式學習技術之應用研究' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='顏士淨' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='深度強化式學習技術之應用研究' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='杭學鳴' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於生成模型的視訊壓縮' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='邱維辰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於生成模型的視訊壓縮' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='蕭旭峰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於生成模型的視訊壓縮' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='彭文孝' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於生成模型的視訊壓縮' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='黃敬群' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於生成模型的視訊壓縮' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='郭峻因' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='應用於ADAS特殊用途無人載具之嵌入式AI深度學習技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='張添烜' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='應用於ADAS特殊用途無人載具之嵌入式AI深度學習技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='阮瑞祥' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='應用於ADAS特殊用途無人載具之嵌入式AI深度學習技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='顏士淨' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='人工智慧在輔助人類學習對局遊戲之應用(2/5~5/5)(1/4)' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='林順喜' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='人工智慧在輔助人類學習對局遊戲之應用(2/5~5/5)(1/4)' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳志昌' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='人工智慧在輔助人類學習對局遊戲之應用(2/5~5/5)(1/4)' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='吳毅成' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='人工智慧在輔助人類學習對局遊戲之應用(2/5~5/5)(1/4)' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='劉吉軒' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='前瞻無人機智慧系統：核心技術研發與情境應用展示' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '主持人' }  );
var person_id = db._query(`for per in person filter per.name=='廖文宏' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='前瞻無人機智慧系統：核心技術研發與情境應用展示' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李蔡彥' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='前瞻無人機智慧系統：核心技術研發與情境應用展示' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='紀明德' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='前瞻無人機智慧系統：核心技術研發與情境應用展示' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='王偉彥' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李宜勳' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='郭建宏' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='林政宏' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李聯旺' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='許陳鑑' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='蔣欣翰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='包傑奇' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究---演示學習型機器人研發' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='許陳鑑' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='王偉彥' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李宜勳' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='郭建宏' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='林政宏' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='蔣欣翰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='包傑奇' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='由人類動作演示引導機器人學習之方法與系統的研究—人類動作及臉部表情三維影像分析系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='王文俊' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='施國琛' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳慶瀚' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='蘇木春' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳翔傑' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='謝易錚' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='林智揚' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='蔡宗漢' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='視障人士的智慧夥伴' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='王家慶' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度智能之口語處理技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='王新民' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度智能之口語處理技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳柏琳' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度智能之口語處理技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='張寶基' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度智能之口語處理技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='蔡宗翰' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度智能之口語處理技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='曹昱' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度智能之口語處理技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='李宏毅' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='基於深度智能之口語處理技術' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='楊明德' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '計畫主持人' }  );
var person_id = db._query(`for per in person filter per.name=='林幸助' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='楊靜瑩' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='許奕婷' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='申雍' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳樹群' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='郭寶錚' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='林芳邦' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='韓仁毓' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
var person_id = db._query(`for per in person filter per.name=='陳琦玲' return per._id`).toArray()[0];
var proj_id = db._query(`for pro in proj filter pro.title=='結合UAV監測之智慧農業栽培支援系統' return pro._id`).toArray()[0]; 
db.participation.save(person_id, proj_id, {position : '共同主持人' }  );
